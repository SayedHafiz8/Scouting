# خطة إضافة رول كشّاف المحترفين — النسخة 2

> **مبنية على فحص الكود الفعلي، وبتلغي النسخة الأولى.**
> الأوامر في النسخة المنصّبة عندك بشرطة: `/speckit-specify` مش `/speckit.specify`.

---

## القرارات المحسومة

| البند | القرار |
|---|---|
| اسم الرول | `proScout` (بديل: `scout` — متاح لأنه مفيش رول بالاسم ده أصلاً) |
| "القسم الثاني" | `league: "professional"` على `Team` و `SeasonMatch` |
| الصلاحيات | قراءة + كتابة تقارير وميديا + إنشاء/تعديل لاعبين + تسجيل حضور المباريات + إدخال نتيجة المباراة (كـattendee يوم المباراة، **[تصحيح 2026-08-22]** زي الكوتش بالظبط) |
| لاعبين `team: null` | يشوفهم لو هو اللي أضافهم |
| حقل الملكية | `createdBy` جديد على `Player` (موصى به) |
| "الرول القديم للكشافين" | مش موجود — البند يتحوّل لـ regression على `coach` و `observer` |

## قرارات لسه مفتوحة (احسمها في `/speckit-clarify`)

1. **حقل الملكية**: `createdBy` جديد ولا إعادة استخدام `coach`؟
2. **`maskObservedForCoach`**: الرول الجديد يشوف حالة `observed` وحقل `observers` ولا يتخفوا عنه زي الكوتش؟
3. **`PATCH /seasonMatches/{id}/status`**: تسجيل الحضور مسموح — بس هل يدخّل نتيجة المباراة كمان؟ (توصية: **لأ**، حضور بس)
4. **`GET /ages`**: مفتوحة بدون `protect` — ثغرة قايمة. توصية: **سجّلها tech debt منفصل** ومتصلحهاش هنا (تصليحها يخالف مبدأ عدم المساس بالسلوك القائم).

---

## السكوب المطلوب (المرجع الفني لكل المراحل)

**اللاعبون:**
```js
{ $or: [
  { team: { $in: <ids فرق league=professional> } },
  { team: null, createdBy: <userId> }
]}
```

**المباريات:** `{ league: "professional" }` — عبر `baseFilterFn` بنفس باترن الأوبزيرفر في `seasonMatchController.js:24-31`

**الفرق:** `{ league: "professional" }` — `GET /teams` حالياً بدون `allowedTo`

**الفئات العمرية:** خارج النطاق تماماً. الرول مش بيفلتر بيها، فمش محتاج `GET /ages` أصلاً.

---

## المرحلة 0 — تمهيدية (بدون أي سلوك جديد)

**ليه موجودة:** أربع مشاكل بنيوية هتفجّر المراحل اللي بعدها لو ماتحلتش الأول.

```
/speckit-specify

مرحلة تمهيدية لإصلاح أساسات تقنية قبل إضافة رول جديد. ممنوع أي تغيير
في سلوك أي رول قائم (coach / observer / admin).

1. تحديث openapi.json: شغّل npm run dump-spec. الملف الحالي ناقص حوالي
   22 operation (Teams بالكامل، dashboard/observer، players/counts،
   players/{id}/observers، مجموعة users، auth/vaultPassword، وغيرها).
   أي جرد endpoints لاحق لازم يتبني على الملف المحدّث.

2. استخراج الرولات لمصدر حقيقة واحد: دلوقتي 'coach'/'admin'/'observer'
   مكتوبين كـ string literals متناثرة، والـ enum في userModel.js:21-25 هو
   الوحيد. أنشئ constant/enum مُصدَّر واستبدل كل الاستخدامات النصية بيه
   في الباك إند. أضف تحقق من role في utils/validation/userValidation.js
   (حالياً userController.js:157 بياخد req.body.role كما هو).

3. إصلاح حلقة الـ redirect: role.guard.ts:16-19 بيعمل fallback على
   /dashboard/coach لأي رول مش admin/observer، والداشبورد دي محمية بـ
   roleGuard(['coach']) → حلقة لا نهائية لأي رول جديد.
   - أنشئ route /unauthorized (مش موجود).
   - غيّر الـ fallback لـ deny-by-default: أي رول غير معروف → /unauthorized.
   - المنطق مكرر في 3 أماكن (role.guard.ts، dashboard.routes.ts:37-41،
     وثالث) — وحّده في مكان واحد.

4. Refactor لـ middlewares/ownership.js من fall-through لـ explicit deny:
   - checkPlayerOwnership دلوقتي: admin → observer → وبعدين "يفترض الباقي
     كوتش" ويقارن player.coach. أي رول جديد بيترفض بالصدفة مش بالتصميم.
   - checkMediaOwnership بيقارن uploadedBy بدون تحقق من الرول — يعني رول
     جديد يشوف الميديا اللي رفعها من غير أي تعريف صريح.
   - حوّل الأربعة (Player/Report/Media/SeasonMatchAttendee) لـ switch صريح
     على الرول مع default: deny.

معايير القبول:
- tests/isolation.test.js يعدي بالكامل بدون تعديل.
- كل اختبارات الانحدار لـ coach و observer و admin تعدي.
- رول وهمي غير معرّف في الاختبارات يترفض بـ 403 من ownership.js صراحةً.
- openapi.json فيه العدد الفعلي للـ operations.
- لا يوجد أي تغيير مرئي في الواجهة.
```

---

## المرحلة 1 — تعريف الرول (بدون أي وصول)

```
/speckit-specify

أضف رول proScout للنظام. في نهاية المرحلة يقدر يعمل login بس مايشوفش
أي بيانات إطلاقاً.

1. أضف proScout لـ enum الرولات في userModel.js وللـ constant المُصدَّر
   من المرحلة 0، وللتحقق في userValidation.js.
2. شغّل npm run dump-spec ثم npm run gen:types عشان UserRole في
   user.model.ts:5 يتحدّث عبر api.generated.ts.
3. تأكد إن buildOwnerScope في apiFeatures.js:82-91 بيرجع MATCH_NOTHING
   للرول الجديد (deny-by-default) — من غير ما تضيفه لـ ownerFields لسه.
4. الأدمن يقدر يسند الرول لمستخدم عبر آلية إسناد الرولات القائمة.

معايير القبول:
- مستخدم بالرول يعمل login بنجاح ويستقبل توكن صالح.
- GET /players يرجع مصفوفة فاضية (MATCH_NOTHING) مش خطأ.
- كل route عليه allowedTo يرجع 403 للرول ده.
- teamRouter.js:18 (GET /teams بدون allowedTo) موثّق كاستثناء معروف
  هيتعالج في المرحلة 2.
- الفرونت: تسجيل الدخول يوجّه لـ /unauthorized (السلوك المتوقع دلوقتي).
- regression كامل على coach و observer و admin.
```

**ملاحظة تنفيذية (اكتُشفت أثناء التنفيذ الفعلي، `/speckit-implement`):**
`GET /players` هي endpoint الوحيدة من بين الأربعة المرشحة (`GET /players`, `GET /players/counts`,
`GET /players/reports/average-ratings`, `GET /seasonMatches`) اللي انضاف لها `proScout` في
`allowedTo`، لأنها الوحيدة اللي بتعدّي فعلياً على `ApiFeature.filter`/`ownerFields` (طبقة النطاق
المركزية) — غيابه من `ownerFields` بيرجّعله `MATCH_NOTHING` فعلاً. التلاتة التانية
(`getCountsByAgeGroup`, `getAverageRatingsForPlayers`, `seasonMatchBaseFilterFor`) عندها منطق
`if/else` مخصص لكل رول بيرجع فلتر فاضي (`{}` = كل البيانات) لأي رول مش معدود فيه صراحةً — لو
اتضاف `proScout` لـ `allowedTo` بتاعهم من غير ما تتصلح، كان هيسرّب كل بيانات المباريات/العداد،
مش يرجّع صفر. اتسابوا 403 لحد ما ينتقلوا لطبقة النطاق المركزية (مرشّحين لتصليح في المرحلة 2 أو
تصليح منفصل، مش الأربعة كتلة واحدة زي ما كان مفترَض هنا).

كمان اتوسّعت قايمة الاستثناء المعروفة (البند 3 فوق) لتشمل التلاتة دول جنب `GET /teams`: كل
واحدة فيهم endpoint موجودة بالفعل وبترجع نتيجة غير مسكوبة (`{}`/فلتر مش شامل proScout) لأي رول
غير معروف — بس لأن `proScout` نفسه اترفض من بوابة `allowedTo` قبل ما يوصلها، مفيش تسريب فعلي
حصل. التسريب كان هيحصل بس لو الحد ده اتفتح من غير تصليح المنطق الداخلي.

---

## المرحلة 2 — طبقة السكوب

**أخطر مرحلة. راجعها سطر بسطر.**

**ملاحظة صريحة (وارثة من المرحلة 1):** `GET /players/counts` و
`GET /players/reports/average-ratings` و `GET /seasonMatches` اتسابوا **403** عمداً
في المرحلة 1 — مش `MATCH_NOTHING` زي `GET /players`. السبب: فروعهم الحالية
(`getCountsByAgeGroup`, `getAverageRatingsForPlayers`, `seasonMatchBaseFilterFor`)
بترجع استعلام **غير مفلتر** (`{}` = كل البيانات) لأي رول مش معدود فيهم صراحةً،
مش `MATCH_NOTHING`. فتحهم لـ `proScout` عبر `allowedTo` من غير سكوب فعلي كان
هيسرّب كل بيانات العدّادات/المباريات، مش يرجّع صفر. **المرحلة 2 لازم تضيف
الثلاثة دول لطبقة السكوب المركزية (`ownerFields` لو أمكن، أو `baseFilterFn` زي
`seasonMatchBaseFilterFor` تحت) قبل ما تفتحهم من `allowedTo` لـ `proScout` — فتح
الـ`allowedTo` من غير السكوب أولاً هو بالظبط الثغرة اللي المرحلة 1 تجنّبتها
عمداً.**

```
/speckit-specify

أضف سكوب البيانات لرول proScout بحيث يستحيل تقنياً وصوله لبيانات خارج
دوري المحترفين.

1. حقل الملكية: أضف createdBy (ObjectId → User) لـ Player مع migration
   تعمل backfill بقيمة coach لكل اللاعبين القائمين. يتعبّى تلقائياً
   بالمستخدم الحالي عند POST /players.

2. سكوب اللاعبين في apiFeatures.js buildOwnerScope:
   { $or: [
       { team: { $in: <ids فرق league=professional> } },
       { team: null, createdBy: <userId> }
   ]}
   استخرج ids فرق المحترفين مرة واحدة لكل طلب مع cache، مش استعلام لكل لاعب.

3. سكوب المباريات: أضف baseFilterFn ترجع { league: "professional" } بنفس
   باترن الأوبزيرفر في seasonMatchController.js:24-31، وعدّل
   seasonMatchBaseFilterFor بحيث ماترجعش {} للرول ده.

4. سكوب الفرق: teamRouter.js:18 عليه protect بس بدون allowedTo — أي رول
   مسجّل بيشوف كل الفرق. أضف سكوب league للرول الجديد على GET /teams
   و GET /teams/:id من غير ما تغيّر سلوك أي رول قائم.

5. ownership.js: أضف فرع proScout صريح في الأربع دوال (من الـ switch
   المعمول في المرحلة 0)، بنفس منطق السكوب فوق.

6. سجّل (log) كل محاولة وصول مرفوضة.

معايير القبول (اختبارات إلزامية):
- طلب لاعب بـ ID تابع لفريق premier → 403/404 مش 200.
- طلب مباراة league=premier بـ ID مباشر → مرفوض.
- تمرير ageGroup أو league في query params → يُدمج مع السكوب ولا يستبدله؛
  النتيجة تفضل professional فقط.
- لاعب team:null أنشأه مستخدم تاني → مش ظاهر.
- لاعب team:null أنشأه هو → ظاهر.
- عدّ النتائج = العدد المحسوب يدوياً من الداتابيز.
- tests/isolation.test.js يعدي، وعدد ومحتوى نتائج coach/observer/admin
  لم يتغير.
```

**ملاحظة تنفيذية (اكتُشفت أثناء التنفيذ الفعلي، `/speckit-implement`):**

المرحلة اتنفّذت بالكامل. المواصفة والخطة والمهام في `specs/003-proscout-data-scope/`.
النتيجة: **492 تست باك إند بيعدّوا (24 ملف)**، `tests/isolation.test.js` **من غير أي تعديل**،
و84 تست فرونت إند. ستة انحرافات عن نص المرحلة فوق، بترتيب الأهمية:

1. **⚠️ الأهم للمراحل الجاية — كل فلتر نطاق لازم يتلفّ في `$and`.** شروط mongoose
   المتسلسلة بتتدمج بـ**آخر واحد يكسب عند تصادم المفتاح، مش بـAND**. و`league` هو مفتاح
   النطاق **و** فلتر مسموح للعميل في `SEASON_MATCH_FILTERS` و`TEAM_FILTERS`. يعني
   `GET /seasonMatches?league=premier` كان بيرجّع الدوري الممتاز كامل للـproScout. مقيس على
   mongoose 9.7.2:
   `find({league:"professional"}).find({league:"premier"})` → `{"league":"premier"}`.
   الحل: `{ $and: [ { league: "professional" } ] }` — `$and` مش في أي وايت ليست فمفيش مفتاح
   من العميل يقدر يتصادم معاه. **أي مرحلة جاية بتضيف نطاق بمفتاح مُدرج في وايت ليست هتقع في
   نفس الفخ.** سكوب الأوبزيرفر القائم نجا بالصدفة لأنه بيفلتر على `$or`/`homeTeam`/`awayTeam`.
   التفاصيل في `research.md` R12، والتستات اللي بتقفله في `proScoutDataScope.test.js`.

2. **`ApiFeature.buildOwnerScope` اتعدّلت** — الخطة كانت بتقول متتلمسش. الافتراض كان إن
   غياب `proScout` من `ownerFields` بيرجّع `MATCH_NOTHING` "كطبقة حماية إضافية" فوق الفلتر
   الأساسي. ده كان **غلط منطقي**: `MATCH_NOTHING ∧ أي حاجة = صفر دايماً`، يعني مش دفاع في
   العمق — منع كامل للـendpoint. الحل: التفرقة بين "الرول غايب من الماب" (→ `MATCH_NOTHING`،
   المنع-بالافتراض محفوظ كما هو) و"موجود بقيمة `null`" (→ `{}`، معناها متسكوب من طبقة تانية).

3. **`average-ratings` مكانتش غير مفلترة.** ملاحظة المرحلة 1 قالت إن التلات endpoints
   بيرجعوا `{}` لأي رول مش معدود. ده صح في `counts` و`seasonMatches` بس. الـthird كانت
   مسكوبة بالفعل على `match.coach = req.user._id` — بس على المحور الغلط (ملكية التقرير مش
   دوري اللاعب). التصليح: تضييق `?ids=` للاعبين داخل النطاق **مع الإبقاء** على قيد الملكية.

4. **ستة حرّاس في `ownership.js` مش أربعة** — `checkSeasonMatchScope` و`checkTeamScope`
   جداد، لأن `GET /seasonMatches/:id` و`GET /teams/:id` مكانش عليهم أي حارس أصلاً.

5. **`GET /players/:id` اتفتح** مع تطبيق `maskObservedForCoach` على الرول (المنع-بالافتراض):
   `observers` مخفي و`observed` بتظهر `pending`. السؤال مفتوح صراحةً للمرحلة 4.

6. **⚠️ القيد C-3 غير قابل للتنفيذ كما هو مكتوب.** الدستور بيطلب منع الرول من `/ages` و
   `/ages/:id` "صراحةً عبر `allowedTo`" — لكن `ageGroupRouter.js:113,116` فيها `.get(getAll)`
   **من غير `protect` إطلاقاً**، فمفيش `req.user` أصلاً عشان `allowedTo` تشتغل عليه.
   المسارات دي متاحة لغير المسجّلين، وده بند الـtech debt رقم 1 تحت
   (`TODO(AGES_UNAUTHENTICATED_READ)`) المستثنى بقرار المالك. **إضافة `protect` مااتعملتش**
   لأنها تغيير سلوك برّه النطاق. التست بيوثّق الوضع الفعلي (200 بتوكن و200 من غيره) عشان
   محدش يفتكر إن المنع موجود وهو مش موجود. **قفل C-3 فعلياً محتاج حل بند الـtech debt الأول.**

كمان: `scripts/syncAllIndexes.js` مااتلمستش — بيعدّي على `Object.entries(mongoose.models)`
فالـindex الجديد اتسجّل تلقائياً.

---

## المرحلة 3 — التنقّل والراوتنج

```
/speckit-specify

فعّل التنقّل المبني على الرولات لـ sidebar.component.ts.

الوضع الحالي: interface NavItem { roles: UserRole[] } معرّف في سطر 10-15
لكنه غير مستخدم إطلاقاً — القائمة HTML مكتوبة بإيد بـ @if. النتيجة إن أي
رول جديد بيشوف Dashboard و Players و Profile تلقائياً (deny-by-default
غير محقق في الفرونت).

المطلوب:
1. حوّل القائمة لمصفوفة NavItem[] تُبنى من الداتا، وفعّل حقل roles.
2. أي عنصر بدون تطابق رول صريح = مخفي (deny-by-default).
3. أعد إنتاج القائمة الحالية لكل رول قائم بدقة 1:1 — ممنوع أي فرق:
   - Dashboard: الكل
   - Players: الكل (+ فروع status للأدمن فقط، سطر 138)
   - Users / Observers / Age Groups: admin فقط
   - My Matches: coach أو observer
   - Profile: الكل
4. أضف proScout لـ: Dashboard, Players, My Matches, Profile.
   لا يُضاف لـ Age Groups إطلاقاً.
5. Route guards: proScout ممنوع من /age-groups و /users و /observers →
   إعادة توجيه لـ /unauthorized.

معايير القبول:
- سنابشوت للقائمة لكل رول قائم مطابق للسلوك قبل التعديل حرفياً.
- سنابشوت لـ proScout يظهر 4 عناصر بالضبط.
- فتح /age-groups مباشرة بالرول الجديد → /unauthorized، والـ API ترفض كمان.
```

**ملاحظة تنفيذية (اكتُشفت أثناء التنفيذ الفعلي، `/speckit-implement`):**
المرحلة اتنفّذت. المواصفة والخطة والمهام في `specs/004-role-based-navigation/`. خمسة انحرافات عن نص المرحلة فوق:

1. **معيار "4 عناصر بالضبط" اتقلّل لـ2 بقرار المالك.** proScout في نهاية المرحلة دي شايف Players وProfile بس، مش الأربعة. السبب: Dashboard وMy Matches وجهتين لسه مرفوضتين له فعلياً — `/dashboard` بيعدّي على `RoleLandingService.landingFor('proScout')` اللي بيرجع `/unauthorized` (مفيش proScout dashboard لسه)، و`/my-matches` محروسة بـ`roleGuard(['coach','observer','admin'])` وendpoints الحضور لسه `allowedTo(coach, observer)` بس. إضافتهم دلوقتي كانت هتنتج لينكات ميتة. اتسجّلوا كـ`DF-001` (Dashboard ← المرحلة 5) و`DF-002` (My Matches ← المرحلة 6) كـfollow-ups ملزمة، كل واحد سمّى بالظبط التعديل المطلوب في مرحلته.
   **تحديث بعد المرحلة 4:** وقوع proScout في الـdefault كان معناه إن تسجيل دخول **ناجح** بينتهي على `/unauthorized`، وصفحة اللاعبين اللي المرحلة 4 خلّصتها تبقى مش قابلة للوصول من الواجهة. اتضاف `case 'proScout'` صريح في `RoleLandingService` بيرجع `['/players']` — **مؤقت**. المرحلة 5 **تعدّل الـcase ده** لـ`['/dashboard/proScout']`، متضيفش case تاني لنفس الرول (فرعين متناقضين في نفس الـswitch، وأولهم بس اللي بيشتغل). التست اللي بيقفل ده في `role-landing.service.spec.ts`.
2. **"فروع status للأدمن، سطر 138" ده وصف غلط لحالة الكود.** سطر 138 هو شرط الـmini-pitch (ملعب الفورميشن)، مش فروع status. `const STATUS_CHILDREN` والـproperty `statusChildren` معرّفين فعلاً بس الـtemplate ماكانش بيقراهم إطلاقاً — القائمة الحالية مفيهاش أي فروع status من الأساس. اتسابوا كما هم (كود ميت، مش جزء من المهمة).
3. **الأدمن مايشوفش My Matches اليوم**، رغم إن الراوت `/my-matches` مسموح له (`roleGuard(['coach','observer','admin'])`). القائمة القديمة كانت بتشرطها بـ`auth.isCoach() || auth.isObserver()` بس. ده تضارب قائم بين القائمة والراوت اتسابناه زي ما هو (مبدأ III) — مش "تصليح" حصل هنا.
4. **الجرد الشامل لـendpoints (بند المبدأ VI) اتعمله استثناء موثَّق مش تنفيذ.** المرحلة دي مابتلمسش أي `allowedTo` ولا مصدر باك إند إنتاجي، فمفيش قرار endpoint اتغيّر لأي رول. الجرد الكامل فاضل مسؤولية المرحلة 7 (`/speckit-checklist`).
5. **`GET /ages` اتثبت إنه **مش مرفوض** لـproScout ولا لأي زائر بلا توكن** — القيد C-3 (`ageGroupRouter.js:113,116` بدون `protect` إطلاقاً). اتسجّل بتست يوثّق السلوك الفعلي (200 في الحالتين) بدل ما يدّعي رفض مش موجود، عشان اختفاء عنصر Age Groups من القائمة محدش يفهمه إنه قفل الباب فعلياً.

---

## المرحلة 4 — صفحة اللاعبين + الكتابة

```
/speckit-specify

صفحة اللاعبين وصلاحيات الكتابة لرول proScout.

القراءة:
1. عرض لاعبي السكوب من المرحلة 2 (فرق المحترفين + لاعبينه بدون فريق).
2. إزالة فلتر الفئة العمرية وأي عمود أو تبويب مرتبط بيها من الصفحة لهذا
   الرول فقط — الفلاتر تفضل زي ما هي لباقي الرولات.
3. صفحة تفاصيل اللاعب: إخفاء أي قسم يخص الفئة العمرية.
4. البحث والترتيب والـ pagination داخل السكوب فقط.
5. GET /players/counts و GET /players/reports/average-ratings مسكوبة كمان.

الكتابة:
6. POST /players و PATCH /players/{id}: حالياً allowedTo("coach") فقط —
   أضف proScout، مع تعبئة createdBy تلقائياً، ومنع إسناد اللاعب لفريق
   خارج league=professional.
7. POST /players/{playerId}/reports و PATCH/DELETE عليها: أضف proScout
   (حالياً coach + observer).
8. POST /players/{playerId}/media و /media/{id}/download: أضف proScout.
9. PATCH /players/{id}/profileImg: أضف proScout.
10. PATCH /players/{id}/observers يفضل admin فقط — ممنوع عليه.

[NEEDS CLARIFICATION] هل يُطبَّق maskObservedForCoach على proScout؟ يعني
هل يتخفى عنه حقل observers وتتحول حالة "observed" لـ "pending" زي الكوتش؟

معايير القبول:
- محاولة إنشاء لاعب بفريق premier → 403.
- محاولة تعديل لاعب خارج السكوب → 403.
- محاولة كتابة تقرير على لاعب خارج السكوب → 403.
- التصدير (لو موجود) يصدّر السكوب فقط.
- سلوك coach و observer في نفس الصفحة والـ endpoints لم يتغير.
```

**ملاحظة تنفيذية (اكتُشفت أثناء التنفيذ الفعلي، `/speckit-implement`):**
المرحلة اتنفّذت. المواصفة والخطة والمهام في `specs/005-proscout-players-write/`. عشر انحرافات عن نص
المرحلة فوق، بترتيب الأهمية:

1. **نص القراءة كله (البنود 1–5) كان متعمول خلاص في المرحلة 2.** `playerScopeFor` في
   `services/scope.js` بتخدم `getAll` و`counts` و`average-ratings` و`checkPlayerOwnership`، و
   `maskObservedForCoach` كانت متطبّقة على `proScout` بالفعل. البنود دي اتنفّذت كـ**تستات تحقّق**
   مش كود جديد — إعادة كتابتها كانت هتخالف المبدأ IV. الـ`[NEEDS CLARIFICATION]` بتاع
   `maskObservedForCoach` كان متجاوب عليه في الكود من المرحلة 2 (انحراف #5 بتاعها)، والمالك أكّد
   نفس الإجابة قبل كتابة المواصفة.
2. **`DELETE /players/{playerId}/reports/{id}` أدمن-أونلي، مش coach+observer** زي ما البند 7 فوق
   بيقول. الكوتش نفسه مايقدرش يمسح تقرير. `proScout` خد `POST` و`PATCH` بس، والرفض اتقفل بتست.
3. **`/media/{id}/download` أدمن-أونلي بقرار من المراجعة الأمنية (`F7d`)** — البند 8 كان بيطلب
   إضافته. إضافته كانت هتدي الرول الجديد صلاحية مالهاش الكوتش ولا الأوبزيرفر وتعكس قرار موثّق.
   **مااتضافش**، وكذلك `DELETE /media/:id` و`PATCH /media/:id/review`.
4. **فريق برّه الدوري بيترفض بـ400 مش 403** (معيار القبول الأول فوق). التفرقة بين "فريق موجود بس
   دوري تاني" و"فريق مش موجود" **oracle** بيخلي الرول يعدّ فرق الدوري التاني بالتخمين، وده اللي
   `checkTeamScope` قفله في المرحلة 2. معيار القبول اتعدّل في المواصفة بدل ما يتساب تضارب صامت،
   وفيه تست بيقارن الردّين حرف بحرف.
5. **`create` كان بيحط `coach = req.user._id` لأي حد** — يعني `proScout` كان هيبقى "الكوتش" بتاع
   اللاعب، وهو حقل `assignPlayerCoach` نفسه بيرفض فيه أي يوزر مش `role: coach`. الحقل بقى بيتمسح
   لأي رول مش كوتش، والنسبة بتروح لـ`createdBy` بس.
6. **مسارات قراءة التقارير والميديا اتفتحت كمان** (مش مذكورة في النص فوق): `players.routes.ts`
   بيخلي `reports` هي الـchild route الافتراضية لصفحة تفاصيل اللاعب، فمن غيرها فتح أي لاعب بالرول
   ده كان بينتهي بصفحة 403.
7. **`AuthService` مكانش فيه `isProScout`** إطلاقاً — المرحلة 3 تجنّبتها بإنها بنت القائمة من مصفوفة
   `NavItem[]`، لكن صفحة اللاعبين بتفلتر على الـcomputeds دي في ~10 مواضع. اتضافت.
8. **بند tech debt رقم 5 (قائمة الرولات في `user-form.component.ts`) دخل النطاق.** من غيره مفيش
   اختبار يدوي ممكن للرول أصلاً. ⚠️ ملاحظة صريحة: `UserRole` **نوع** مشتق من `openapi.json` ومالوش
   قيمة وقت التشغيل، فالقايمة **مفحوصة وقت الترجمة مش مشتقّة تلقائياً** — إضافة رول لـ`openapi.json`
   مش هتملاها لوحدها.
9. **⚠️ `POST /users/:id/players` ميّت لكل الرولات — بند tech debt جديد.** `playerRouter` متمركّب
   مرتين (`userRouter.js:482`)، و`setUserIdToBody` بيحقن `req.body.coach` من الـURL **قبل**
   `createValidate`، فـ`lockField("coach")` بيرفضه بـ400 دايماً. الافتراض الأولي في `research.md`
   R14 إن ده ثغرة تصعيد صلاحيات كان **غلط** واتصحّح هناك بعد القياس: الـ`lock` بيمسكها فعلاً.
   الـ`delete` في `create` اتساب كدفاع في العمق بس. الراوت نفسه مااتصلحش (تغيير سلوك لرول قائم،
   برّه النطاق) — `TODO(NESTED_PLAYER_CREATE_DEAD)`.
10. **جرد الـendpoints الكامل اتعمل هنا مش اتأجّل** (المبدأ VI بيطلبه من أي مرحلة بتمس الصلاحيات).
    83 operation في `specs/005-proscout-players-write/contracts/endpoint-inventory.md`: 24 مسموح/مسكوب،
    57 مرفوض، 2 (`/ages`) لا ده ولا ده. القيد **C-3 لسه مفتوح** — إيقاف طلب `/ages` من صفحة اللاعبين
    تغيير **نية** مش قفل باب، وفيه تست بيوثّق إن الـendpoint بيرجع 200 بتوكن ومن غيره.

---

## المرحلة 5 — الداشبورد

```
/speckit-specify

داشبورد رول proScout.

1. أنشئ GET /dashboard/proScout بنفس باترن /dashboard/coach و
   /dashboard/observer الموجودين.
2. كل الإحصائيات محسوبة عبر طبقة السكوب من المرحلة 2 — ممنوع كتابة
   استعلامات إحصائية جديدة تتجاوزها.
3. المؤشرات: عدد لاعبيه، المباريات القادمة في دوري المحترفين، آخر النتائج،
   تقاريره الأخيرة.
4. ممنوع أي كارت أو رسم بياني يخص الفئات العمرية.
5. سلوك واضح للحالة الفارغة.
6. dashboard.routes.ts: أضف مسار الرول الجديد للـ mapping بعد إصلاح
   الـ fallback في المرحلة 0.

معايير القبول:
- كل رقم يساوي نظيره المحسوب يدوياً من بيانات league=professional.
- أرقام /dashboard/coach و /dashboard/observer و /dashboard/admin لم تتغير.
```

---

## المرحلة 6 — المباريات وتسجيل الحضور

```
/speckit-specify

صفحة المباريات لرول proScout.

1. GET /seasonMatches و GET /seasonMatches/{id}: مباريات league=professional
   فقط عبر baseFilterFn من المرحلة 2.
2. تفاصيل المباراة: التشكيل والأحداث والنتيجة، مع إخفاء أي ربط بالفئة العمرية
   في الواجهة (الحقل نفسه required في seasonMatchModel.js:4-8 فيفضل في
   الداتا، بس مايتعرضش).
3. الفلاتر: التاريخ والبطولة والخصم — بدون فلتر الفئة العمرية.
4. POST و DELETE /seasonMatches/{id}/attend: أضف proScout (حالياً
   coach + observer)، مع تحقق ownership إن المباراة داخل السكوب.
5. **[محسوم — تصحيح 2026-08-22]** PATCH /seasonMatches/{id}/status: يُفتح
   لـproScout **كمان**، مش يفضل مقفول. نفس قيود الكوتش/الأوبزيرفر بالظبط:
   attendee مسجّل على المباراة + يوم المباراة نفسه بس (الأدمن مستثنى)، حسب
   المنطق القائم في seasonMatchController.js:104-117 — القيد ده بيتفعّل
   تلقائياً بمجرد إضافة proScout لـallowedTo لأنه مبني على
   `role !== ADMIN`, مش محتاج فرع كود جديد.
   ~~توصية سابقة بالمنع كانت هنا — اتلغت.~~
6. checkSeasonMatchAttendee في ownership.js: أضف فرع proScout صريح.
7. **(مسجّلة من مراجعة المرحلة 5 — /speckit-analyze)** اختبار مركزي واحد
   لوجهات الرفض/الهبوط لكل رول: أنشئ ملف اختبار واحد (مثلاً
   `role-landing-destinations.spec.ts` أو مشابه) بيثبت وجهة الهبوط/الرفض
   الصحيحة لكل تركيبة (رول × حالة وصول/رفض) في مصفوفة واحدة — بدل ما تتوزّع
   الحقيقة على أكتر من ملف spec منفصل (زي `role-landing.service.spec.ts` و
   `role.guard.spec.ts` و specs المراحل القديمة). أي تغيير مستقبلي على أي
   landing destination (زي ما حصل في المرحلة 4 و5 مع proScout) لازم يفشّل
   الاختبار المركزي ده فوراً، بدل ما يتسرّب بصمت ويسيب مستند مرحلة قديمة
   (زي SC-005 في specs/004-role-based-navigation/spec.md) بيوثّق سلوك بايت.

معايير القبول:
- مباراة league=premier مش ظاهرة ولا قابلة للوصول بـ ID مباشر.
- تسجيل حضور على مباراة خارج السكوب → 403.
- proScout attendee يدخل نتيجة مباراة داخل السكوب يوم المباراة نفسه → 200،
  ونفس المحاولة برّه السكوب أو برّه اليوم أو من غير attendee → مرفوضة، نفس
  رسالة الرفض بتاعة الكوتش/الأوبزيرفر بالظبط.
- سلوك coach و observer في المباريات لم يتغير (خصوصاً
  seasonMatchController.js:104-117 لإدخال النتيجة يوم المباراة).
- الاختبار المركزي لوجهات الهبوط/الرفض (بند 7) موجود ومغطّي كل رول قائم +
  proScout، ومربوط بمصدر الحقيقة الوحيد (RoleLandingService) لا نسخة موازية.
```

**ملاحظة تنفيذية (اكتُشفت أثناء التنفيذ الفعلي، `/speckit-implement`):**
المرحلة اتنفّذت بالكامل. المواصفة والخطة والمهام في `specs/008-proscout-matches-attendance/`. النتيجة:
**638 تست باك إند بيعدّوا (28 ملف)**، `tests/isolation.test.js` **من غير أي تعديل** (15/15)، و**166 تست
فرونت إند بيعدّوا**. أهم الانحرافات والاكتشافات أثناء التنفيذ، بترتيب الأهمية:

1. **قرار المرحلة الأصلي ("منع إدخال النتيجة") كان محسوم بالفعل قبل التنفيذ** — التصحيح حصل في مراجعة
   المستخدم على المواصفة (`/speckit-specify` ثم تصحيح لاحق)، فمفيش انحراف وقت `/speckit-implement` هنا؛
   `PATCH /:id/status` اتفتح لـproScout زي ما هو موثّق في `FR-006` بنفس قيد الكوتش/الأوبزيرفر بالظبط.

2. **⚠️ فرع proScout في `checkSeasonMatchAttendee` كان "شبه جاهز" مش "مقفول تمامًا"** — من المرحلة 2،
   الفرع كان بيحسب فحص النطاق (`inScope`) صح، وبعدين **يرفض دايماً بغض النظر عن النتيجة** — placeholder
   متعمّد وموثّق في تعليق الكود نفسه. التصليح كان إضافة فحص عضوية `attendees` (نفس شكل فرع
   الكوتش/الأوبزيرفر) والفرع الفعلي على نتيجة `inScope` بدل الرفض الثابت — مش كتابة منطق جديد من الصفر.
   تست موجود من المرحلة 2 (`T042` في `proScoutDataScope.test.js`) كان **مكتوب عمدًا عشان ينقلب هنا**؛
   تعليقه بيسمّي المرحلة 6 صراحةً. اتعدّل في مكانه (rename + قلب 2 assertion + إضافة حالة "in-scope
   بس مش attendee" الناقصة) بدل ما يتكتب ملف جديد يكرره.

3. **مسارات `/attend` (POST/DELETE) معندهاش أي حارس ownership خالص** قبل المرحلة دي — `allowedTo` بس،
   من غير `checkSeasonMatchScope` ولا أي فحص نطاق. لو `PRO_SCOUT` كان اتضاف لـ`allowedTo` من غير حارس،
   كان هيقدر يحضر أي مباراة بأي ID برّه نطاقه. الحل: إعادة استخدام `checkSeasonMatchScope` نفسه (نفس
   الحارس بتاع `GET /:id`) على مسارات الحضور — تعريف واحد للنطاق، مش نسخة تانية منه (Principle IV).

4. **⚠️ `vi.useFakeTimers()` العادي بيعلّق التستات اللي بتعمل HTTP request حقيقي عبر supertest** — الفورم
   الافتراضي بيجمّد كل الـtimers مش بس `Date`، وده بيوقف أي حاجة داخلية في Express/Mongoose بتعتمد على
   `setTimeout` حقيقي (زي الـheartbeat)، فبيعلّق التست 20 ثانية وبعدين الحالة بتسرّب للتستات اللي بعده.
   الحل: `vi.useFakeTimers({ toFake: ['Date'] })` بس — بيسيب كل الـtimers الحقيقية شغالة وبيجمّد `Date`
   بس. غير موثّق في `research.md` (اتكتشف وقت التنفيذ مش التخطيط)، موثّق في `tasks.md` T014 لأي حد
   هيمدّ الملف ده مستقبلاً.

5. **`MyMatchesComponent` كان بالفعل role-agnostic في منطق الحضور وإدخال النتيجة** — `canEnterResult`،
   `isAttending`، `canToggleAttend` كلها مبنية على `auth.isAdmin()` وعضوية `attendees`، مش على
   `isCoach()/isObserver()`. المرحلة دي محتاجتش تلمسهم إطلاقًا — بس إخفاء عمود الفئة العمرية وقفل تبويب
   الدوري على "professional" (مع تعديل الـcolspan للصفوف الموسّعة عشان العمود المخفي).

6. **بند 7 (الاختبار المركزي لوجهات الهبوط/الرفض) اتنفّذ في**
   `frontend/src/app/core/auth/role-landing-destinations.spec.ts` — مصفوفة `EXPECTED` واحدة (رول →
   وجهة) بتغذّي محورين: `RoleLandingService.landingFor` مباشرة، و`roleGuard` فعليًا عبر مسار مرفوض.
   `role.guard.spec.ts` القديم كان بيكرّر نفس السلاسل النصية (`'/dashboard/coach'`, `'/dashboard/proScout'`,
   …) في 11 assertion منفصلة — اتصلح بحساب القيمة المتوقعة حيًّا من `RoleLandingService` (عبر `new
   RoleLandingService()` مباشرة، مش `TestBed.inject` تاني، تجنّبًا لتعارض إعادة تهيئة الـTestBed جوه نفس
   التست) بدل النص الحرفي — فالملف بقى **مايقدرش ينحرف** عن مصدر الحقيقة بنفس الطريقة اللي حصلت في
   المرحلتين 4 و5.

7. **FR-003 (فلاتر التاريخ/البطولة/الخصم) اتصحّحت قبل التنفيذ** عبر `/speckit-analyze` (F1/F2) — الصفحة
   القائمة معندهاش أي فلتر من دول لأي رول، والمواصفة كانت بتفترض قدرة مش موجودة أصلاً. تصحيح الطلب حصل
   في spec.md قبل ما تبدأ الكتابة، فمفيش كود اتلغى أو اتغيّر بسببه هنا — مسجّل هنا للسياق بس.

8. **اختبار التحقق اليدوي في المتصفح (quickstart.md §6) ماتعملش** في السيشن ده — يحتاج تشغيل
   dev servers وتفاعل بصري، برّه نطاق البيئة دي. كل سلوك كان هيتفحص فيه متغطّي فعليًا بالتستات الآلية
   (G-1…G-13 على مستوى HTTP، وDOM assertions في `my-matches.component.spec.ts`).

كمان: `openapi.json` و`api.generated.ts` اتحدّثوا (`dump-spec` + `gen:types`) بديف صغير ومحدود (+9/-3
و+13/-3 سطر على الترتيب) مطابق تمامًا للتلات عمليات اللي اتغيرت.

---

## المرحلة 7 — التصليب

```
/speckit-checklist

أنشئ تشيك ليست أمان لرول proScout تغطي:

1. جرد كامل من openapi.json المحدّث (بعد dump-spec) لكل operation، وتحديد
   استجابة proScout لكل واحدة: مسموح / 403 / مسكوب. الافتراضي لازم يكون رفض.
2. اختبار سلبي لكل route خاص بالفئات العمرية والمستخدمين والأوبزيرفرز
   والتقييمات (coachEvaluations / observerEvaluations).
3. اختبارات e2e في مجلد e2e/: تسجيل دخول بالرول، التأكد إن الفئات العمرية
   مش في القائمة، ومحاولة الوصول المباشر عبر URL مرفوضة.
4. التأكد إن أي endpoint جديد يُضاف مستقبلاً مرفوض افتراضياً لهذا الرول
   (اختبار على مستوى الراوتر مش على مستوى كل route).
5. مراجعة logs محاولات الوصول المرفوضة.
6. tests/isolation.test.js موسّعة لتشمل الرول الجديد.
7. regression كامل على coach و observer و admin.
```

**ملاحظة تنفيذية (اكتُشفت أثناء التنفيذ الفعلي، `/speckit-implement`):**

المرحلة اتنفّذت عبر دورة speckit كاملة مستقلة (`specs/009-proscout-hardening/`)، بدل ما تتنفّذ كـ`/speckit-checklist` مباشر زي النص الأصلي — قرار المالك، عشان بنود المرحلة فعلياً مهام تنفيذ/تحقق لا أسئلة جودة مواصفة. أهم الانحرافات والاكتشافات، بترتيب الأهمية:

1. **⚠️ بند 4 ("منع endpoint جديد افتراضياً") كان مبني على افتراض غلط عن السلوك القائم.**
   غياب `allowedTo` من راوت **مبيرفضش** أي رول — بيسمح لكل رول مسجّل (Constitution
   Principle II صريحة في ده). التصميم الأصلي افترض العكس. الحل: ضمانتان منفصلتان
   وصادقتان بدل ضمانة وهمية واحدة — (أ) unit test مباشر على `allowedTo(...)` نفسها
   يثبت إنها بترفض أي رول مش في قايمتها، (ب) فحص تطابق عدد الـoperations في
   `Backend/routes/*.js` مع عدد صفوف جرد المرحلة، فراوت جديد من غير صف جرد بيفشّل
   الفحص حتى لو الراوت نفسه لسه مفتوح فعلياً. التفاصيل في `research.md` R4.

2. **⚠️ بند 1 (الجرد) كان فيه غلط واقعي في نص الـspec الأول اتصحّح أثناء التنفيذ:**
   `GET /ages` و`GET /ages/:id` اتفترضوا "proScout مرفوض عبر allowedTo" — لكن قراءة
   `ageGroupRouter.js` مباشرة بتأكد إن الراوتين دول من غير `protect` ولا `allowedTo`
   خالص: proScout بياخد 200 زي أي حد تاني وزي طلب من غير توكن خالص. `POST /ages`
   بس هو المرفوض (admin-only). كمان `GET /teams`/`GET /teams/:id` اتفترضوا "OPEN"
   زي `/ages` بالظبط، لكنهم فعلياً `protect`-gated و**مسكوبين** لـproScout
   (`teamScopeFor`/`checkTeamScope`، المرحلة 2) — مش open. الجرد النهائي
   (`contracts/endpoint-inventory.md`) بيوثّق الحقيقة الفعلية: **92 عملية**، مش الـ83
   المذكورة في جرد المرحلة 4/5 (تناقض قائم في مجموع صفوف الوثيقة القديمة نفسها،
   موثّق كـdiscrepancy معروف مش اتصلح بالقوة).

3. **⚠️ إضافة `logRoleDenial` (بند 5، التسجيل) كسرت 30 تست قائم من المرحلتين 4/5**
   (`proScoutPlayersWrite.test.js`, `proScoutDataScope.test.js`) — `500` بدل `403`.
   السبب: الملفين بيعملوا `vi.mock('.../accessLog.js', ...)` بموك ناقص مابيصرّحش
   بالـexport الجديد، فـ`allowedTo()` كان بيدّي `TypeError` بدل الرفض المقصود.
   التصليح: تحديث الموك في الملفين ليصرّح بالـexport الجديد — مش تغيير في أي
   assertion قائم. اكتشاف مباشر لقيمة تشغيل full regression suite فعلياً بعد أي
   تغيير إنتاجي، مش الاكتفاء بمراجعة الكود. التفاصيل في `research.md` R8.

4. **بند 2 (اختبار سلبي للأوبزيرفرز) اتصحّح**: مفيش راوتر مستقل "للأوبزيرفرز" —
   `PATCH /players/:id/observers` (في `playerRouter.js`) هو المسار الوحيد الإضافي
   برّه سطح `userRouter.js` (المغطّى أصلاً ببند المستخدمين).

5. **بند 6 (isolation.test.js) اتحدّد نطاقه لـ`GET /players` بس** — العقد ده بيحكم
   `ApiFeature.filter()` تحديداً، وسطح query-param القابل للحقن عند proScout في
   التقارير/الميديا/المباريات/الفرق ضيق جداً (أغلبه مغطّى في `proScoutDataScope
   .test.js` من المرحلة 2 أصلاً) — إضافة سيناريوهات مصطنعة كانت هتخالف مبدأ
   "لا نصوص زيادة بدون داعي".

6. **بند 3 (E2E) اتكتب وماتتشغّلش فعلياً في السيشن دي** — محتاج backend + frontend
   شغّالين live، مش متاحين هنا. الملفات (`proscout-hardening.spec.ts`،
   `loginAsProScout`، تمديد `seed.js`) جاهزة ومراجَعة، بس مش مؤكَّدة ضد تطبيق حي.

كمان: `npm run dump-spec` و`npm run gen:types` اتشغّلوا وأكّدوا صفر تغيير في الشكل
(متوقّع — المرحلة دي مابتضيفش أي endpoint جديد).

---

## Backlog — بعد المرحلة 7 (خارج نطاق أي مرحلة حالية)

بنود اتسجّلت أثناء تنفيذ **المرحلة 4c** (`specs/006-admin-professional-lens/`)، ومؤجَّلة عن قصد
لحد ما تتحدد كـmini-stage مستقلة بنفس دورة `/speckit-specify` → `/speckit-plan` → ... :

1. **صفحة "المدربين" تعرض الكوتشز والـproScouts مع بعض.** feature جديدة، مش امتداد لصفحة قائمة —
   محتاجة spec مستقلة تحسم: هل هي شاشة واحدة بفلتر رول، ولا شاشتين متجاورتين؟ هل الأدمن بس اللي
   يشوفها؟ هل فيه أي إجراء (تعطيل/تعديل) موحّد بين الرولين ولا كل رول له إجراءاته القائمة؟

2. **عدسة "دوري المحترفين" (4c) تعرض اسم الـproScout المسؤول عن كل لاعب.** **مؤجَّل**، مش مضاف
   لـ4c قبل الدمج — رغم قرب المسافة المنطقية. السبب:

   - **مش تعديل بسيط فعلياً.** `Player.createdBy` بيتسجّل لكل لاعب (كل الرولات، مش الـproScout
     بس) من المرحلة 2، لكن `GET /players` النهاردة **مبيعملوش populate ولا بيرجّعه خالص** — لا
     في الـ`getAll` ولا في أي مسار تاني. يعني الإضافة دي بتفتح حقل مالوش أي استهلاك حالياً من أي
     endpoint، مش بس بتعرض حقل موجود بالفعل.
   - أسئلة نطاق حقيقية لسه مفتوحة: هل الاسم ده يتعرض لكل اللاعبين (كوتش كمان له `createdBy`)
     ولا للمحترفين بس؟ هل فيه أي قناع/سكوب لازم يتطبّق (زي `PLAYER_ADMIN_ONLY_LENSES` القائمة
     لـ`coach`/`observers`)؟ محتاجة إجابة قبل أي سطر كود، مش قرار تنفيذي وقت الكتابة.
   - **4c اتقفلت وخلاص** — 613/613 باكإند، 137/137 فرونت إند، `isolation.test.js` بدون أي تعديل،
     واختبار SC-004 الحاسم عدّى. إعادة فتحها لإضافة غير مخطَّطة بتاخد فرصة إعادة تشغيل بوابات
     الجودة كلها لحاجة ملهاش spec لسه.

   **التوصية:** يتسجّل كبند backlog منفصل (زي ما هو هنا) ويتعامل معاه كـ**4d** بدورة speckit كاملة
   لوحدها، بنفس النمط اللي 4c اتعاملت بيه مع 4b — مش يتضاف داخل نطاق 4c بعد ما قفلت.

### بنود اتسجّلت أثناء تنفيذ المرحلة 6 (`specs/008-proscout-matches-attendance/`)

مؤجَّلة عن قصد لحد ما تتحدد كـmini-stage مستقلة بعد ما المرحلة 7 تخلص — مش تعديل فوري على مرحلة مقفولة:

1. **[= بند 4d فوق، مسجّل بالفعل]** اسم الـproScout المسؤول عن كل لاعب يظهر في عدسة "دوري المحترفين"
   عند الأدمن. نفس البند رقم 2 المسجّل فوق من مراجعة 4c — بيتكرر ذكره هنا بس عشان أي حد بيقرا خطوة
   المرحلة 6 يلاقي كل البنود المؤجَّلة في نفس المكان، مش لأنه بند جديد. التفاصيل والأسئلة المفتوحة زي
   ما هي فوق.

2. **[جديد] داشبورد proScout (`GET /dashboard/proScout`, المرحلة 5) مالوش كاردز
   Selected/Rejected/Pending** الموجودة في داشبورد الكوتش والأوبزيرفر. الكاردز دي بتتحسب من
   `Player.status` (`selected`/`pending`/`rejected`، والكوتش بيحسب `observed` كـ`pending` كمان —
   `dashboardController.js` حوالي سطر 135-143 و207-216) مجمّعة على `coach`. proScout عنده نفس مفهوم
   الحالة على تقاريره الكشفية (لاعبينه بنفس السكوب)، فمنطقياً محتاج نفس نوع الكارد — بس المصدر الحالي
   لسه مش موصّل لـ`getProScoutDashboard` (اللي بيرجّع بس `totalPlayers`/`upcomingMatchesCount`/
   `totalReports`/المباريات/التقارير الأخيرة، من غير أي تقسيم بالحالة).

   أسئلة نطاق لسه مفتوحة قبل أي سطر كود: هل التقسيم بيتحسب على *كل* لاعبيه في السكوب (فرق المحترفين +
   لاعبينه بدون فريق) ولا على اللي هو أنشأهم (`createdBy`) بس؟ هل فيه فرق بين "لاعب اتقيّم" و"لاعب لسه
   محتاج تقييم" زي التمييز القائم بين الكوتش والأوبزيرفر، ولا proScout بيتعامل معاهم كلهم بنفس المنطق؟

   **التوصية:** يتسجّل كبند backlog منفصل ويتعامل معاه بدورة speckit كاملة لوحده بعد المرحلة 7، بنفس
   النمط بتاع 4d فوق — مش تعديل على `specs/007-proscout-dashboard/` بعد ما قفلت.

---

## المرحلة 11 — تضييق سكوب اللاعبين لـproScout إلى createdBy فقط

**⚠️ تعديل جوهري على قرار المرحلة 2 — مش بند backlog، ومش تكملة لأي مرحلة قائمة.**
المرحلة 2 (`specs/003-proscout-data-scope/`) حسمت إن سكوب اللاعبين هو
`{ team: professional } OR { team: null, createdBy: self }` — يعني proScout بيشوف
كل لاعبي فرق دوري المحترفين، بغض النظر مين أنشأهم. القرار الجديد بيلغي الفرع الأول
تماماً: **proScout يشوف بس اللاعبين اللي هو نفسه أنشأهم (`createdBy`)، أياً كان
فريقهم**، والأدمن بس هو اللي يشوف الباقي. زي المرحلة 2 بالظبط، دي محتاجة دورة speckit
كاملة ومراجعة بشرية سطر بسطر — مش تعديل سريع.

### القرار المحسوم

| السكوب | الوضع الحالي | القرار الجديد |
|---|---|---|
| اللاعبين (قائمة + تفاصيل + داشبورد) | `team: professional` OR `team: null, createdBy: self` | `createdBy: self` بس — بلا استثناء |
| المباريات (`seasonMatchScopeFor`) | `league: professional` | **بلا تغيير** |
| الفرق (`teamScopeFor`) | `league: professional` | **بلا تغيير** (لسه محتاجة للعرض واختيار الفريق وقت إنشاء لاعب) |

### جرد فعلي لكل نقطة بتستخدم `playerScopeFor` أو نفس منطقه — وأثر التضييق على كل واحدة

**1. تعريف السكوب نفسه — [`services/scope.js:82-97`](../Backend/services/scope.js#L82-L97)**
`playerScopeFor` هي مصدر الحقيقة (Principle IV). التعديل هنا: حذف فرع `team: {$in: teamIds}`
بالكامل، والإبقاء على `{ createdBy: req.user._id }` فقط (بدون شرط `team: null` أصلاً — الشرط
ده بقى بلا معنى لأن `createdBy` وحده كافي). `professionalTeamIds()` نفسها **تفضل موجودة** —
لسه مستخدمة في `teamScopeFor`/`checkTeamScope` (بلا تغيير) وفي التحقق وقت الكتابة (بند 10 تحت).

**2. `playerController.js:159` — `getCountsByAgeGroup`**
الفلاج `professional` (Stage 4c) والعدّاد الإجمالي بيتحسبوا على `finalMatch` اللي فيها
السكوب. الأثر: مباشر — عدّاد proScout هيرجع بس لاعبينه هو، مش كل لاعبي دوري المحترفين.
مفيش تعديل منطق مطلوب هنا (بتستهلك `playerScopeFor` زي ما هي) — التأثير كله في `scope.js`.

**3. `playerController.js:295` — `getAll` (قائمة اللاعبين الرئيسية)**
نفس الشيء: بتستهلك `playerScopeFor` مباشرة. تأثير مباشر على القائمة اللي proScout بيشوفها،
وعلى populate الـ`createdBy` للأدمن (specs/010) اللي **مش متأثر** لأنه مربوط بـ`role===ADMIN`
مش بالسكوب.

**4. `middlewares/ownership.js:55-66` — `checkPlayerOwnership` (فرع proScout)**
⚠️ **مش استدعاء لـ`playerScopeFor` — نسخة معاد كتابتها يدوياً في الذاكرة** (code-review
fix #4 القديم، لتفادي round-trip تاني). لازم تتعدّل **مع** `scope.js` في نفس الـPR، وإلا
هتنحرف الاثنتان عن بعض (بالظبط الخطر اللي Principle IV بيحذر منه). التعديل: حذف فرع
`player.team && teamIds.some(...)`، الإبقاء على `player.createdBy?.equals(req.user._id)` بس.

**5. `middlewares/ownership.js:81-88` — `playerInProScoutScope` (helper مشترك)**
مستخدمة من `checkReportOwnership` (بند 6) و`checkMediaOwnership` (بند 7). نفس الملاحظة:
نسخة يدوية تانية من نفس منطق السكوب، لازم تتزامن مع التعديل في `scope.js` و`checkPlayerOwnership`
مع بعض — **تلات نسخ من نفس القاعدة في نفس الملف/الملفات، مش نسخة واحدة**. فرصة حقيقية لتوحيدها
في دالة واحدة مُصدَّرة من `scope.js` بدل التكرار الثلاثي، بس ده قرار تصميم يتاخد وقت `/speckit-plan`
مش افتراض هنا.

**6. `middlewares/ownership.js:123-135` — `checkReportOwnership` (فرع proScout)**
⚠️ **تشديد حقيقي، مش مجرد إعادة استخدام.** الفحص الحالي: `isAuthor AND belongsToPlayer AND
inScope(player)` — أي proScout كتب تقرير على لاعب في فريق محترفين (حتى لو مش هو اللي أنشأ
اللاعب) بيقدر يعدّل/يقرا تقريره هو. بعد التضييق، `inScope` هتبقى `createdBy فقط`، يعني
**proScout ممكن يفقد الوصول لتقرير هو نفسه كاتبه**، لو اللاعب مش من إنشائه. ده سيناريو حقيقي
تحت السكوب الحالي (لاعب فريق محترفين، أي proScout يقدر يكتب عليه تقرير من المرحلة 4)، مش
افتراضي. **قرار منتج مطلوب صراحة قبل الكتابة**: هل ده مقصود (التقرير برضه ملك اللاعب مش الكاتب)،
ولا لازم استثناء لسيناريو "أنا كاتب التقرير حتى لو مش صاحب اللاعب"؟

**7. `middlewares/ownership.js:185-208` — `checkMediaOwnership` (فرع proScout)**
نفس التشديد بالظبط، على الميديا بدل التقارير: `isUploader AND belongsToPlayer AND inScope(player)`.
نفس السؤال المفتوح في بند 6 مطلوب يتحسم بنفس الإجابة (اتساق) — رافع ميديا مش صاحب اللاعب.

**8. `scoutingReportController.js:302` — `getAverageRatingsForPlayers`**
بتستخدم `playerScopeFor` لتضييق `?ids=` قبل الـpipeline، وبعدها بتفلتر كمان على
`report.coach = req.user._id` (ملكية التقرير، مستقل). بعد التضييق: التقاطع بين "لاعب أنشأته"
و"تقرير كتبته" هيبقى أضيق من دلوقتي — نتيجة متوقعة ومتسقة مع بند 6، مفيش منطق إضافي مطلوب هنا،
بس الأعداد المرجعة هتتغيّر وتحتاج تستات جديدة.

**9. `dashboardController.js:255-323` — `getProScoutDashboardData` (المرحلة 5)**
⚠️ **محتاجة مراجعة خاصة كما طلبت.** `playerScope` بتتحسب هنا وتُستخدم في:
- `totalPlayers = Player.countDocuments(playerScope)` → هيهبط لعدد اللي هو أنشأهم بس.
- `scopedPlayerIds = Player.find(playerScope).distinct("_id")` → نفس التضييق.
- `reportFilter = { coach: req.user._id, player: {$in: scopedPlayerIds} }` → `totalReports`
  و`recentReports` هيتأثروا: تقرير كتبه proScout على لاعب مش من إنشائه (سيناريو بند 6) هيختفي
  من الداشبورد كمان، اتساقاً مع اختفائه من `checkReportOwnership`.
- `matchScope` (upcoming/latestResults) **مش متأثرة إطلاقاً** — القرار الجديد صريح إن سكوب
  المباريات يفضل زي ما هو (كل دوري المحترفين)، فالمباريات القادمة والنتائج الأخيرة هتفضل بتعرض
  كل مباريات الدوري، مش بس مباريات لاعبينه. **ده عدم اتساق مقصود بقرار صريح** (مباريات ≠ لاعبين)،
  لازم يتوثّق في الـspec كسلوك متعمَّد مش سهو، وإلا هيتقرا كـbug وقت المراجعة.
- **رابط مباشر بالـbacklog المسجّل آخر المستند** (كاردز Selected/Rejected/Pending): تضييق
  السكوب لـ`createdBy` بيحسم سؤال النطاق المفتوح هناك تلقائياً — التقسيم هيتحسب على نفس
  `playerScope` الجديد (= لاعبينه اللي هو أنشأهم)، فمفيش غموض "كل السكوب ولا createdBy بس"
  بعد المرحلة دي.

**10. الكتابة — `POST /players` و`PATCH /players/:id` (المرحلة 4)**
`teamScopeFor`/`checkTeamScope` (التحقق إن الفريق المُسنَد داخل دوري المحترفين) **بلا تغيير** —
proScout لسه يقدر يعمل create/patch للاعب على أي فريق محترفين. الفرق: بعد الإنشاء، اللاعب هيبقى
مرئي لمُنشئه بس (بحكم `createdBy` تلقائياً)، مش لأي proScout تاني — ده تنفيذ مباشر للقرار
الجديد، مفيش تعديل كود إضافي مطلوب في مسار الإنشاء نفسه.

⚠️ **الأثر الأخطر على البيانات القائمة (تحذير محتاج قرار migration صريح):**
أي لاعب موجود دلوقتي على فريق دوري محترفين لكن `createdBy` بتاعه **مش** proScout (لاعب
أنشأه كوتش قبل ما ينتقل لفريق محترف، أو استوردته الأدمن، أو `createdBy` فاضي من داتا قديمة
قبل المرحلة 2) — **هيبقى غير مرئي تماماً لأي proScout بعد المرحلة دي**، حتى لو كان ظاهر
لكل الـproScouts قبلها. مفيش proScout هيقدر يشوفه أو يعدّله أو يكتب عليه تقرير — غير الأدمن.
ده سؤال نطاق لازم يتحسم في `/speckit-clarify` قبل التنفيذ: هل ده مقبول كما هو (orphans تتعالج
يدوياً من الأدمن)، ولا لازم migration بتـbackfill/تُسند `createdBy` لبعض الحالات؟

**11. عدسة "دوري المحترفين" (Stage 4c، `specs/006-admin-professional-lens/`)**
✅ **مفحوصة بدقة — مفيش تأثير كود مباشر.** الأدمن مش بيمر على `playerScopeFor` أصلاً:
الدالة بترجع `{}` فوراً لأي رول غير `PRO_SCOUT` ([`scope.js:88`](../Backend/services/scope.js#L88))،
وفرع الأدمن في `getAll`/`getCountsByAgeGroup` (`playerController.js:106-117`) بيبني الفلتر
من `?coach=`/`?observer=`/`isProfessional` مباشرة، مستقل تماماً عن سكوب proScout. يعني
عدسة الأدمن هتفضل بترجع **كل** لاعبي دوري المحترفين زي ما هي دلوقتي، بلا تغيير. المطلوب هنا
مجرد **regression** (تشغيل `adminProfessionalLens.test.js` كما هو، مش تعديل)، مش مراجعة تصميم —
رغم إن الافتراض الأولي كان إنها "مبنية على الفرض القديم"، الفحص أثبت العكس.

**12. اسم proScout المسؤول عن اللاعب (Stage 4d، `specs/010-professional-lens-creator/`)**
✅ **نفس الاستنتاج — مفيش تأثير كود.** الـpopulate بتاع `createdBy` مربوط بـ`role===ADMIN`
([`playerController.js:305-307`](../Backend/controllers/playerController.js#L305-L307))، مش
بسكوب proScout. **لكن القيمة الوظيفية للفيتشر دي بتزيد بشكل كبير بعد المرحلة 11**: هي هتبقى
تقريباً **الطريقة الوحيدة** اللي بيها الأدمن يعرف مين مسؤول عن أي لاعب في دوري المحترفين —
قبل المرحلة 11 كان أي proScout يقدر "يشوف" اللاعب حتى لو مش بتاعه، دلوقتي `createdBy` بقى
حرفياً خط تقسيم الملكية الوحيد. مفيش تعديل كود، بس يستاهل ملاحظة في الـspec إن أهمية الفيتشر
دي اتغيّرت بعد المرحلة دي.

### خلاصة الفحص

| الملف | التغيير المطلوب |
|---|---|
| `services/scope.js` | تعديل `playerScopeFor` — حذف فرع `team` |
| `middlewares/ownership.js` | 3 نسخ يدوية من نفس المنطق (`checkPlayerOwnership`, `playerInProScoutScope`) لازم تتزامن معاها |
| `playerController.js`, `dashboardController.js`, `scoutingReportController.js` | مفيش تعديل منطق — بتستهلك `playerScopeFor` جاهزة، التأثير في البيانات المرجعة بس |
| `specs/006`, `specs/010` (4c/4d) | مفيش تعديل كود — regression بس، رغم الافتراض الأولي |

### أسئلة مفتوحة لازم تتحسم في `/speckit-clarify` قبل أي سطر كود

1. **Migration**: لاعبين دوري محترفين موجودين حالياً بـ`createdBy` مش proScout — يتسابوا orphans
   (مرئيين للأدمن بس) ولا يتعملهم backfill؟
2. **تقارير/ميديا proScout كتبها/رفعها على لاعب مش من إنشائه** (بند 6/7) — يفقد الوصول ليها ولا
   استثناء "أنا الكاتب/الرافع" يفضل شغال حتى لو اللاعب برّه سكوبه الجديد؟
3. **عدم اتساق سكوب المباريات مقابل اللاعبين في الداشبورد** (بند 9) — يتوثّق صراحة في الـspec
   كسلوك متعمَّد؟

**التلاتة اتحسموا كلهم Option A في نفس اليوم (2026-08-23)**: مفيش migration للـorphans، سكوب
اللاعب بيغلب على التأليف (proScout بيفقد الوصول لتقريره/ميديته لو اللاعب خرج من نطاقه)، والتباين
بين المباريات (كامل الدوري) واللاعبين (createdBy بس) في الداشبورد اتوثّق كتصميم مقصود صراحة.

---

**ملاحظة تنفيذية (اكتُشفت أثناء التنفيذ الفعلي، `/speckit-implement`):**

المرحلة اتنفّذت بالكامل عبر دورة speckit كاملة (`specs/011-proscout-createdby-scope/`)، بما فيها
تعديل دستوري (C-4 اتلغى واتستبدل، `constitution.md` v1.0.2 → v1.1.0). النتيجة النهائية:
**726 تست باك إند بيعدّوا (33 ملف)**، `tests/isolation.test.js` **من غير أي تعديل**، و`dump-spec`/
`gen:types` بصفر ديف (متوقّع — المرحلة دي مبتضيفش ولا endpoint جديد). أهم الاكتشافات، بترتيب
الأهمية:

1. **⚠️ أخطر اكتشاف — تضييق `playerScopeFor` كسر تستات في ملفين تانيين مش على الخطة أصلاً**
   (`proScoutPlayersWrite.test.js` من المرحلة 4/5، و`proScoutDashboard.test.js` من المرحلة 5): 22
   تست فشلوا فور تعديل `services/scope.js`. السبب الجذري في الاتنين: fixtures helper (`inScopePlayer()`
   في الأول، وinline `createPlayerDoc` في التاني) كانت بتحط اللاعب على فريق دوري محترفين من غير
   `createdBy` خالص — بتعتمد ضمنياً على الفرع القديم (`team: {$in: teamIds}`) اللي المرحلة دي
   ألغته. التصليح: إضافة `createdBy: scout.user._id` لكل fixture، بنفس نمط `proScoutDataScope.test.js`
   (T004). **الدرس**: جرد المرحلة 11 الأصلي (`docs/scout-pro-plan-v2.md` قبل التنفيذ) راجع كل
   استهلاكات `playerScopeFor` في كود *الإنتاج*، لكن ماراجعش fixtures التستات القديمة اللي بتفترض
   شكل السكوب القديم ضمنياً — دي فجوة حقيقية في نطاق الجرد الأولي، مش خطأ تنفيذ.

2. **تست واحد في `proScoutPlayersWrite.test.js` كانت آليته نفسها اتلغت، مش بس fixture بايت** —
   "a player leaving the league locks its own report" كان بينقل اللاعب لفريق premier عشان يحاكي
   خروجه من السكوب. بعد المرحلة دي، نقل الفريق **مالوش أي أثر على السكوب خالص** (السكوب بقى
   `createdBy` بس). اتصلّح بمحاكاة إعادة إسناد `createdBy` مباشرة في الداتابيز بدل نقل الفريق —
   نفس نية التست الأصلية (سكوب اللاعب هو الحامل مش التأليف)، بس بالآلية الصح.

3. **جدول 4c/4d (specs/006, specs/010) اتأكد إنه مش متأثر فعلياً، مش افتراض** — شغّلت
   `adminProfessionalLens.test.js` كامل، 29/30 عدّى من غير لمس، وتست واحد بس فشل ("the filter is a
   no-op for proScout") لنفس سبب البند 1 بالظبط (fixture من غير `createdBy`) — مش لأن عدسة الأدمن
   نفسها اتأثرت. اتصلّح بنفس النمط، 30/30 دلوقتي.

4. **T014 (سيناريو "اسمين مختلفين لعدسة الأدمن") اتلاقى موجود بالفعل** — `adminProfessionalLens.test.js`
   عندها تست من المرحلة 4d (`specs/010`) بيثبت بالظبط نفس المطلوب (لاعبين اتنين، منشئين مختلفين،
   أسماء صح ومختلفة) وهو مستقل تماماً عن سكوب الـproScout (الأدمن بيتخطاه بالكامل). مفيش تست جديد
   اتضاف — كان هيكرر تغطية موجودة.

5. **T022 (التحقق اليدوي في المتصفح، quickstart.md §3-5) ماتعملش فعلياً في السيشن دي** — محتاج
   تشغيل dev servers وتفاعل بصري، برّه نطاق البيئة دي. كل سيناريوهات §3-4 (المشي بين اتنين proScout،
   فحص عدسة الأدمن) متغطّية آلياً بنفس الـassertions عبر supertest في `proScoutCreatedByScope.test.js`
   (T005-T019). §5 (فحص واجهة الفرونت إند بصرياً) فجوة حقيقية لسه مفتوحة — مفيش كود UI جديد للمرحلة
   دي، فمفيش إيه يُختبر غير التحمّل السليم لنتيجة أضيق (empty state)، لكن التأكيد البصري نفسه محتاج
   جلسة تفاعلية.

كمان: `T001` (تأكيد الفرع) اكتشف إن مفيش `before_specify` git hook في المشروع (`.specify/extensions.yml`
مش موجود)، فالتنفيذ كله حصل على الفرع القائم `phase-5b-dashboard-status-cards` بدل فرع مخصص
`011-proscout-createdby-scope` — اتسجّل صراحة بدل ما يتفرّع تلقائياً من غير علم المستخدم.

---

## ملاحظات تشغيلية

- **conversation جديدة في Claude Code لكل مرحلة**، تبدأ بـ:
  `اقرا docs/scout-pro-plan-v2.md وهنشتغل على المرحلة X`
- **branch منفصل لكل مرحلة**، و`tests/isolation.test.js` يتشغّل قبل كل merge.
- المرحلة 0 والمرحلة 2 هما الوحيدتان اللي محتاجين مراجعة بشرية سطر بسطر.
- بعد أي تعديل على الرولات أو الـ routes: `npm run dump-spec` ثم `npm run gen:types`.

## Tech debt مسجّل (خارج نطاق الخطة)

1. `ageGroupRouter.js:112-116` — `GET /ages` و `GET /ages/:id` بدون `protect` إطلاقاً، متاحين لغير المسجّلين.
2. `teamRouter.js:18` — `GET /teams` بـ `protect` بدون `allowedTo`.
3. `userValidation.js` — مافيهاش تحقق من `role` (اتحلّت في المرحلة 0).
4. `Player.teamName` كنص حر بديل عن `Player.team` (`playedModel.js:50`) — مصدر تشوّش في السكوب.
5. ~~`user-form.component.ts:170-173` — قائمة اختيار الرول مكتوبة بإيد بثلاث خيارات ثابتة.~~
   **اتحلّت في المرحلة 4.** القائمة بقت مبنية من `const ROLE_OPTIONS: readonly UserRole[]`، فالأدمن
   يقدر يسند `proScout` من الواجهة. باقي قيد: `UserRole` نوع مش قيمة وقت تشغيل، فالفحص وقت الترجمة
   مش اشتقاق تلقائي.
6. **`TODO(NESTED_PLAYER_CREATE_DEAD)`** — `POST /users/:id/players` بيرجع 400 لكل الرولات:
   `setUserIdToBody` بيحقن `req.body.coach` قبل `createValidate`، و`lockField("coach")` بيرفضه.
   يا الـmiddleware غلط يا الـlock. اتقاس في المرحلة 4 واتساب كما هو (إصلاحه تغيير سلوك لرول قائم).

## المرحلة 4c — إصلاح فجوة بعد 4b (خارج نطاق الخطة الأصلية)

**مش من ضمن الخطة دي.** المرحلة 4b سابت لاعبي المحترفين (`isProfessional: true`) بدون `ageGroup`
إطلاقاً (استثناء C-4، الدستور v1.0.2)، فمابقوش تابعين لأي كارد في شبكة الفئات العمرية عند الأدمن،
ومابقاش عند الأدمن طريق مقصود يوصلهم بيه — الطريق الوحيد اللي كان شغال كان بالصدفة (عدسة "بدون كوتش"،
أثر جانبي لقرار R14 في المرحلة 4). التفاصيل والتنفيذ الكامل في `specs/006-admin-professional-lens/`
و`PR-DESCRIPTION.md` بتاعتها.
